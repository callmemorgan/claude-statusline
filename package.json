{
	"name": "claude-statusline-dev",
	"version": "0.0.0",
	"private": true,
	"description": "Dev tooling (lint/format) for the claude-statusline repo. Not published.",
	"scripts": {
		"lint": "biome check",
		"lint:fix": "biome check --write",
		"format": "biome format --write",
		"format:check": "biome format",
		"go:lint": "golangci-lint run ./...",
		"go:fmt": "gofmt -l . | grep -v '^\\.\\(claude\\|worktrees\\)/' || true",
		"go:vet": "go vet ./...",
		"go:test": "go test ./...",
		"check": "biome check && golangci-lint run ./... && go vet ./... && go test ./..."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0"
	}
}
